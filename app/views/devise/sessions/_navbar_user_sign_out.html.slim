= form_for(:user, url: destroy_user_session_path, method: :delete, html: {class: 'navbar-form signout-form'}) do |f|
  .form-group.email
    = current_user.email
  = f.submit t('sign_out'), class: "btn btn-primary"